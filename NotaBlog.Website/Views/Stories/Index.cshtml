@model IEnumerable<NotaBlog.Api.StoryLeadViewModel>

@{
    ViewData["Title"] = "Index";
}

@foreach(var item in Model)
{
    <h2>@item.Title</h2>
    <small>@item.Published</small>
    @Html.Raw(item.LeadContent);
    <hr/>
}

